import { IndexLetterPipe } from './index-letter.pipe';

describe('IndexLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
